language: ruby
sudo: false
before_install:
  - bundle install
cache: bundler
bundler_args: --without documentation development production
rvm:
  - 2.2
  - ruby-head
matrix:
  allow_failures:
    - rvm: ruby-head
  fast_finish: true
